# 🔄 Миграция типов объектов ИС

## ✅ Что было сделано

Проект был полностью переведен на **официальную классификацию объектов интеллектуальной собственности РФ** согласно Гражданскому кодексу.

### Старая классификация (упрощенная):
- `trademark` - Товарный знак
- `patent` - Патент
- `copyright` - Авторское право
- `software` - ПО
- `design` - Дизайн
- `other` - Прочее

### Новая классификация (полная, ГК РФ):

#### 📚 **1. Результаты творческой деятельности** (Авторское и смежные права)
- `literary_work` - Произведения науки, литературы и искусства
- `software` - Программы для ЭВМ
- `database` - Базы данных
- `performance` - Исполнения
- `phonogram` - Фонограммы
- `broadcast` - Сообщения радио- и телепередач

#### 🏭 **2. Объекты промышленной собственности**
- `invention` - Изобретения
- `utility_model` - Полезные модели
- `industrial_design` - Промышленные образцы
- `plant_variety` - Селекционные достижения
- `topology` - Топологии интегральных микросхем

#### 🏷️ **3. Средства индивидуализации**
- `trademark` - Товарные знаки и знаки обслуживания
- `trade_name` - Фирменные наименования
- `commercial_designation` - Коммерческие обозначения
- `geographical_indication` - Географические указания и НМПТ

#### 🔐 **4. Нетрадиционные объекты**
- `know_how` - Секреты производства (ноу-хау)

#### ⚙️ **Прочее**
- `other` - Другое

---

## 📝 Обновленные файлы

### Backend:
1. **`backend/app/models/ip_objects.py`** - Модель IPType с полной классификацией
2. **`backend/app/schemas/ip_object.py`** - Схемы валидации
3. **`backend/app/schemas/valuation.py`** - Добавлены поля `subtype` и `subtype_metrics`
4. **`backend/app/core/ip_types.py`** ⭐ **НОВЫЙ** - Справочник констант и утилит

### Frontend:
1. **`frontend/lib/api.ts`** - Обновлены типы IPType и IP_TYPES_RU
2. **`frontend/lib/ipTypes.ts`** ⭐ **НОВЫЙ** - TypeScript константы для типов ИС
3. **`frontend/pages/valuation/index.tsx`** - Селектор типов с группировкой по категориям
4. **`frontend/pages/ip-objects.tsx`** - Обновлена логика расчета сроков действия

---

## 🎯 Новые возможности

### 1. **Группированный селектор типов**
Теперь при выборе типа объекта ИС пользователь видит **4 категории** с группировкой:
```
📚 Результаты творческой деятельности
  - Произведения науки, литературы и искусства
  - Программы для ЭВМ
  - Базы данных
  ...

🏭 Объекты промышленной собственности
  - Изобретения
  - Полезные модели
  ...
```

### 2. **Специфические метрики для подтипов**
Для каждого типа объекта доступны **уникальные метрики оценки**:

#### Для `literary_work`:
- Объем аудитории / Охват
- Уникальность контента
- Стоимость перепроизводства

#### Для `software`:
- Сложность архитектуры / Кода
- Масштабируемость
- Зрелость стека (отсутствие долга)

#### Для `invention`:
- Наукоемкость (R&D глубина)
- Защита от обхода (Work-around)
- Сложность реализации

#### Для `trademark`:
- Узнаваемость бренда
- Лояльность аудитории (LTV)
- География присутствия

### 3. **Корректный расчет сроков действия**
- **Авторские права** (literary_work, software, database и др.) - 70 лет
- **Изобретения** (invention) - 20 лет
- **Остальные объекты** - 10 лет

---

## 🚀 Как использовать

### Создание нового объекта ИС:
1. Перейдите в **"Мои IP-объекты"**
2. Выберите тип из **группированного списка**
3. Система автоматически применит правильные сроки действия

### Оценка объекта:
1. Перейдите в **"Оценка ИС"** (Капитализация 2.0)
2. Выберите тип объекта из **4 категорий**
3. Если доступно, выберите **подкатегорию** (например, для ПО: SaaS, Mobile App, AI)
4. Заполните **специфические метрики** для выбранного подтипа
5. Получите профессиональную оценку с PDF-отчетом

---

## ⚠️ Миграция существующих данных

### Автоматическая миграция:
Старые типы будут автоматически сопоставлены:
- `patent` → `invention`
- `copyright` → `literary_work`
- `design` → `industrial_design`

### Ручная корректировка:
Рекомендуется вручную проверить и обновить типы существующих объектов для более точной классификации.

---

## 📚 Справочная информация

### Полезные функции (backend):
```python
from app.core.ip_types import get_category_for_type, get_label_for_type

# Получить категорию для типа
category = get_category_for_type("software")  # "copyright"

# Получить человекочитаемое название
label = get_label_for_type("invention")  # "Изобретения"
```

### Полезные функции (frontend):
```typescript
import { getCategoryForType, getLabelForType } from '@/lib/ipTypes';

const category = getCategoryForType("software");  // "copyright"
const label = getLabelForType("trademark");  // "Товарные знаки и знаки обслуживания"
```

---

## 🎨 Интерфейс

### Страница оценки:
- ✅ Группированный селектор с эмодзи-иконками категорий
- ✅ Динамические подтипы в зависимости от выбранного типа
- ✅ Специфические метрики с подсказками (tooltips)
- ✅ Автоматический расчет с учетом типа объекта

### Страница "Мои объекты":
- ✅ Корректное отображение всех 22 типов объектов
- ✅ Правильный расчет оставшегося срока действия
- ✅ Группировка по категориям в селекторе

---

## 🔧 Техническая информация

### База данных:
Поле `type` в таблице `ip_objects` теперь принимает значения из расширенного enum (22 значения вместо 6).

### API:
Все эндпоинты поддерживают новые типы без изменений в сигнатурах.

### Валидация:
Pydantic схемы автоматически валидируют новые типы на бэкенде.

---

## 📞 Поддержка

При возникновении вопросов или проблем с миграцией:
1. Проверьте логи контейнеров: `docker-compose logs backend`
2. Убедитесь, что база данных обновлена: `docker-compose restart backend`
3. Очистите кэш браузера и перезагрузите фронтенд

---

**Дата миграции:** 18 января 2026  
**Версия:** 2.0.0  
**Статус:** ✅ Завершено
